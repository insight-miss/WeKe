import { RainBowDirective } from './rain-bow.directive';

describe('RainBowDirective', () => {
  it('should create an instance', () => {
    const directive = new RainBowDirective();
    expect(directive).toBeTruthy();
  });
});
